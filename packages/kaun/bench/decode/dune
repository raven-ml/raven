(executable
 (name bench_decode)
 (libraries nx rune kaun thumper unix))

; Not attached to runtest on purpose: run with `dune build @bench`.

(rule
 (alias bench)
 (locks %{project_root}/bench)
 (action
  (progn
   (run %{exe:bench_decode.exe})
   (diff? kaun_decode.thumper kaun_decode.thumper.corrected))))
