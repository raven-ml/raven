; The suites jit without naming a device and assert CPU semantics; pin the
; process-wide default so they do not auto-select a GPU backend.

(env
 (_
  (env-vars
   (DEV CPU))))

(tests
 (names
  test_kaun
  test_init
  test_fn
  test_loss
  test_metric
  test_checkpoint
  test_layers
  test_conv
  test_stateful
  test_attention
  test_data
  test_data_parallel
  test_jit_state
  test_half
  test_rope
  test_decoder
  test_structures)
 (modules
  (:standard \ test_cache_index_metal test_decode_devices))
 (package kaun)
 (libraries kaun rune vega nx nx.dtype windtrap unix))

(tests
 (names test_cache_index_metal)
 (modules test_cache_index_metal)
 (package kaun)
 (enabled_if
  (= %{system} macosx))
 (libraries kaun rune nx windtrap))

; Over CPU:1..CPU:4; it reads tolk's estimate of the bytes a replay moves.

(test
 (name test_decode_devices)
 (modules test_decode_devices)
 (package kaun)
 (libraries kaun rune nx tolk zarith windtrap))
