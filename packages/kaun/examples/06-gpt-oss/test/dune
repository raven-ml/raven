; The model's modules are the example's.

(copy_files ../{moe,gpt_oss,layer_loop}.{ml,mli})

(tests
 (names test_layer_loop test_structures)
 (package kaun)
 (libraries kaun kaun.hf jsont rune nx nx.quant unix windtrap)
 (action
  (setenv
   RUNE_JIT_DEBUG
   1
   (run %{test}))))
