; nx-oxcaml needs an OxCaml switch and has its own dune-project, so its build
; rules are not ours to run. Its sources stay visible so the backend contract
; can be checked against them (packages/nx/test/test_oxcaml_backend.ml).

(data_only_dirs nx-oxcaml)
