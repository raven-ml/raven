(executable
 (name bench_link)
 (enabled_if (= %{system} macosx))
 (libraries unix tolk tolk.uop tolk.metal thumper.clock))

; Standalone measurement, like ../runtime: build with --profile release,
; then run the executable directly. No machine-specific timing threshold.
