#include <metal_stdlib>
using namespace metal;
kernel void test(device float* data0_-1, uint3 gid [[threadgroup_position_in_grid]], uint3 lid [[thread_position_in_threadgroup]]) {
  *(data0_-1+0) = ((float)(INFINITY));
  *(data0_-1+1) = ((float)(NAN));
}
