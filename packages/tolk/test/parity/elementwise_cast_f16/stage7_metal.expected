#include <metal_stdlib>
using namespace metal;
struct args_t { device half* data0_256; device float* data1_256; device float* data2_256; };
kernel void elementwise_cast_f16(constant args_t& args [[buffer(0)]], uint3 gid [[threadgroup_position_in_grid]], uint3 lid [[thread_position_in_threadgroup]]) {
  device half* data0_256 = args.data0_256; device float* data1_256 = args.data1_256; device float* data2_256 = args.data2_256;
  int gidx0 = gid.x; /* 256 */
  half val0 = (*(data0_256+gidx0));
  float val1 = (*(data1_256+gidx0));
  *(data2_256+gidx0) = (((float)(val0))+val1);
}
