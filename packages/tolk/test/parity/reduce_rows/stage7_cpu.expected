void reduce_rows(float* restrict data0_-1, float* restrict data1_-1, const int data2_) {
  float buf0[1];
  *(buf0+0) = 0.0f;
  for (int Ridx1 = 0; Ridx1 < 32; Ridx1++) {
    float val0 = (*(data0_-1+((data2_<<5)+Ridx1)));
    *(buf0+0) = ((*(buf0+0))+val0);
  }
  *(data1_-1+data2_) = (*(buf0+0));
}
