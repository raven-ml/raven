__kernel void reduce_rows(__global float* data0_-1, __global float* data1_-1) {
  float buf0[1];
  int gidx0 = get_group_id(0); /* 8 */
  *(buf0+0) = 0.0f;
  for (int Ridx1 = 0; Ridx1 < 32; Ridx1++) {
    float val0 = (*(data0_-1+((gidx0<<5)+Ridx1)));
    *(buf0+0) = ((*(buf0+0))+val0);
  }
  *(data1_-1+gidx0) = (*(buf0+0));
}
