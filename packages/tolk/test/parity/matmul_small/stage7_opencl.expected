__kernel void matmul_small(__global float* data0_-1, __global float* data1_-1, __global float* data2_-1) {
  float buf0[1];
  int gidx0 = get_group_id(0); /* 4 */
  int gidx1 = get_group_id(1); /* 4 */
  int alu0 = (gidx1<<2);
  *(buf0+0) = 0.0f;
  for (int Ridx2 = 0; Ridx2 < 4; Ridx2++) {
    float val0 = (*(data0_-1+(alu0+Ridx2)));
    float val1 = (*(data1_-1+(gidx0+(Ridx2<<2))));
    *(buf0+0) = ((*(buf0+0))+(val0*val1));
  }
  *(data2_-1+(gidx0+alu0)) = (*(buf0+0));
}
