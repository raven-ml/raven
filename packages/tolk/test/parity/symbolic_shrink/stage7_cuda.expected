#define INFINITY (__int_as_float(0x7f800000))
#define NAN (__int_as_float(0x7fffffff))
template <class T, class F> __device__ __forceinline__ T tg_bitcast(F v) { union U { F f; T t; }; U u; u.f = v; return u.t; }
extern "C" __global__ void __launch_bounds__(1) r_28start_pos2B129n3(float* data0_1, float* data1_128, const int data2_) {
  float buf0[1];
  *(buf0+0) = 0.0f;
  for (int Ridx0 = 0; Ridx0 < (data2_+1); Ridx0++) {
    float val0 = (*(data1_128+Ridx0));
    *(buf0+0) = ((*(buf0+0))+val0);
  }
  *(data0_1+0) = (*(buf0+0));
}
