void r_28start_pos2B129n1(float* restrict data0_1, float* restrict data1_128, const int data2_) {
  float buf0[1];
  *(buf0+0) = 0.0f;
  for (int Ridx0 = 0; Ridx0 < (data2_+1); Ridx0++) {
    float val0 = (*(data1_128+Ridx0));
    *(buf0+0) = ((*(buf0+0))+val0);
  }
  *(data0_1+0) = (*(buf0+0));
}
