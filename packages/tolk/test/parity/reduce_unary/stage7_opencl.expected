__kernel void r_16n7(__global float* data0_1, __global float* data1_16) {
  float buf0[1];
  __attribute__ ((aligned (16))) __local float buf1[16];
  int lidx0 = get_local_id(0); /* 16 */
  float val0 = (*(data1_16+lidx0));
  *(buf1+lidx0) = val0;
  barrier(CLK_LOCAL_MEM_FENCE);
  *(buf0+0) = 0.0f;
  for (int Ridx101 = 0; Ridx101 < 16; Ridx101++) {
    float val1 = (*(buf1+Ridx101));
    *(buf0+0) = ((*(buf0+0))+val1);
  }
  if ((lidx0==0)) {
    *(data0_1+0) = -sqrt((*(buf0+0)));
  }
}
