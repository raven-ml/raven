#define INFINITY (__int_as_float(0x7f800000))
#define NAN (__int_as_float(0x7fffffff))
template <class T, class F> __device__ __forceinline__ T tg_bitcast(F v) { union U { F f; T t; }; U u; u.f = v; return u.t; }
extern "C" __global__ void __launch_bounds__(16) r_16n3(float* data0_1, float* data1_16) {
  float buf0[1];
  __shared__ __align__(16) float buf1[16];
  int lidx0 = threadIdx.x; /* 16 */
  float val0 = (*(data1_16+lidx0));
  *(buf1+lidx0) = val0;
  __syncthreads();
  *(buf0+0) = 0.0f;
  for (int Ridx101 = 0; Ridx101 < 16; Ridx101++) {
    float val1 = (*(buf1+Ridx101));
    *(buf0+0) = ((*(buf0+0))+val1);
  }
  if ((lidx0==0)) {
    *(data0_1+0) = -sqrt((*(buf0+0)));
  }
}
