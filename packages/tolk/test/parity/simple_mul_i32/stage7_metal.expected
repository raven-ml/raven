#include <metal_stdlib>
using namespace metal;
kernel void test(device int* data0_-1, device int* data1_-1, device int* data2_-1, uint3 gid [[threadgroup_position_in_grid]], uint3 lid [[thread_position_in_threadgroup]]) {
  int val0 = (*(data0_-1+0));
  int val1 = (*(data1_-1+0));
  *(data2_-1+0) = (val0*val1);
}
