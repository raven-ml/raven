__kernel void test(__global float* data0_1) {
  __attribute__ ((aligned (16))) __local float buf0[256];
  *(buf0+0) = 0.0f;
  barrier(CLK_LOCAL_MEM_FENCE);
  float val0 = (*(buf0+0));
  *(data0_1+0) = val0;
}
