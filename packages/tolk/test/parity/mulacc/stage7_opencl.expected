__kernel void r_16_16n5(__global float* data0_1, __global float* data1_256, __global float* data2_256) {
  float buf0[1];
  float buf1[1];
  __attribute__ ((aligned (16))) __local float buf2[16];
  int lidx0 = get_local_id(0); /* 16 */
  *(buf0+0) = 0.0f;
  for (int Ridx0 = 0; Ridx0 < 16; Ridx0++) {
    int alu1 = ((lidx0<<4)+Ridx0);
    float val0 = (*(data1_256+alu1));
    float val1 = (*(data2_256+alu1));
    *(buf0+0) = ((*(buf0+0))+(val0*val1));
  }
  *(buf2+lidx0) = (*(buf0+0));
  barrier(CLK_LOCAL_MEM_FENCE);
  *(buf1+0) = 0.0f;
  for (int Ridx101 = 0; Ridx101 < 16; Ridx101++) {
    float val2 = (*(buf2+Ridx101));
    *(buf1+0) = ((*(buf1+0))+val2);
  }
  if ((lidx0==0)) {
    *(data0_1+0) = (*(buf1+0));
  }
}
