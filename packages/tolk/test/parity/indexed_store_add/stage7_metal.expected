#include <metal_stdlib>
using namespace metal;
struct args_t { device float* data0_128; device int* data1_5; device float* data2_40; };
kernel void indexed_store_add(constant args_t& args [[buffer(0)]], uint3 gid [[threadgroup_position_in_grid]], uint3 lid [[thread_position_in_threadgroup]]) {
  device float* data0_128 = args.data0_128; device int* data1_5 = args.data1_5; device float* data2_40 = args.data2_40;
  int gidx0 = gid.x; /* 8 */
  for (int Ridx1 = 0; Ridx1 < 5; Ridx1++) {
    int val0 = (*(data1_5+Ridx1));
    bool alu0 = ((-1<val0)&(val0<16));
    float val1 = (alu0?*(data0_128+(gidx0+(val0<<3))):0.0f);
    float val2 = (*(data2_40+(gidx0+(Ridx1<<3))));
    if (alu0) {
      *(data0_128+(gidx0+(val0<<3))) = (val1+val2);
    }
  }
}
