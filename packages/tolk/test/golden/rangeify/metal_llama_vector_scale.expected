#include <metal_stdlib>
using namespace metal;
kernel void E_2_2_4n2(device float* data0_16, device float* data1_16, device float* data2_2, device float* data3_8, uint3 gid [[threadgroup_position_in_grid]], uint3 lid [[thread_position_in_threadgroup]]) {
  int lidx0 = lid.x; /* 2 */
  float val0 = (*(data2_2+lidx0));
  int lidx1 = lid.y; /* 2 */
  int alu0 = (lidx1<<2);
  int alu1 = ((lidx0<<3)+alu0);
  float4 val1 = (*((device float4*)((data1_16+alu1))));
  float4 val2 = (*((device float4*)((data3_8+alu0))));
  *((device float4*)((data0_16+alu1))) = float4((val1.x*val0*val2.x),(val1.y*val0*val2.y),(val1.z*val0*val2.z),(val1.w*val0*val2.w));
}
