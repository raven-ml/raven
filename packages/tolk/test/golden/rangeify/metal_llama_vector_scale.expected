#include <metal_stdlib>
using namespace metal;
struct args_t { device float* data0_16; device float* data1_16; device float* data2_2; device float* data3_8; };
kernel void E_2_2_4(constant args_t& args [[buffer(0)]], uint3 gid [[threadgroup_position_in_grid]], uint3 lid [[thread_position_in_threadgroup]]) {
  device float* data0_16 = args.data0_16; device float* data1_16 = args.data1_16; device float* data2_2 = args.data2_2; device float* data3_8 = args.data3_8;
  int lidx0 = lid.x; /* 2 */
  float val0 = (*(data2_2+lidx0));
  int lidx1 = lid.y; /* 2 */
  int alu0 = (lidx1<<2);
  int alu1 = ((lidx0<<3)+alu0);
  float4 val1 = (*((device float4*)((data1_16+alu1))));
  float4 val2 = (*((device float4*)((data3_8+alu0))));
  float alu2 = (1/val0);
  *((device float4*)((data0_16+alu1))) = float4((val1.x*alu2*val2.x),(val1.y*alu2*val2.y),(val1.z*alu2*val2.z),(val1.w*alu2*val2.w));
}
