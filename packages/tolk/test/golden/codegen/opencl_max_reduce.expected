__kernel void max_reduce(__global float* data0_-1, __global float* data1_-1) {
  float buf0[1];
  *(buf0+0) = ((float)(-INFINITY));
  for (int Ridx0 = 0; Ridx0 < 64; Ridx0++) {
    float val0 = (*(data0_-1+Ridx0));
    float alu1 = (((*(buf0+0))<val0)?val0:(*(buf0+0)));
    *(buf0+0) = alu1;
  }
  *(data1_-1+0) = (*(buf0+0));
}
