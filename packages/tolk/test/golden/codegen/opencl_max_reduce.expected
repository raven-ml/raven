__kernel void max_reduce(__global float* data0_64, __global float* data1_1) {
  float buf0[1];
  *(buf0+0) = ((float)(-INFINITY));
  for (int Ridx0 = 0; Ridx0 < 64; Ridx0++) {
    float val0 = (*(data0_64+Ridx0));
    float val1 = (*(buf0+0));
    float alu1 = ((val1<val0)?val0:val1);
    *(buf0+0) = alu1;
  }
  *(data1_1+0) = (*(buf0+0));
}
