__kernel void matmul_small(__global float* data0_16, __global float* data1_16, __global float* data2_16) {
  float buf0[1];
  int gidx0 = get_group_id(0); /* 4 */
  int gidx1 = get_group_id(1); /* 4 */
  int alu0 = (gidx1<<2);
  *(buf0+0) = 0.0f;
  for (int Ridx2 = 0; Ridx2 < 4; Ridx2++) {
    float val0 = (*(data0_16+(alu0+Ridx2)));
    float val1 = (*(data1_16+(gidx0+(Ridx2<<2))));
    *(buf0+0) = ((*(buf0+0))+(val0*val1));
  }
  *(data2_16+(gidx0+alu0)) = (*(buf0+0));
}
