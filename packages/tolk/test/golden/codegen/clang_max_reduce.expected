void max_reduce(float* restrict data0_-1, float* restrict data1_-1) {
  float buf0[1];
  *(buf0+0) = ((float)(-__builtin_inff()));
  for (int Ridx0 = 0; Ridx0 < 64; Ridx0++) {
    float val0 = (*(data0_-1+Ridx0));
    float alu1 = (((*(buf0+0))<val0)?val0:(*(buf0+0)));
    *(buf0+0) = alu1;
  }
  *(data1_-1+0) = (*(buf0+0));
}
