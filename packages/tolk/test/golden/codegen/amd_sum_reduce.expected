extern "C" __attribute__((global)) void __attribute__((amdgpu_flat_work_group_size(1, 1))) sum_reduce(float* data0_256, float* data1_1) {
  float buf0[1];
  *(buf0+0) = 0.0f;
  for (int Ridx0 = 0; Ridx0 < 256; Ridx0++) {
    float val0 = (*(data0_256+Ridx0));
    *(buf0+0) = ((*(buf0+0))+val0);
  }
  *(data1_1+0) = (*(buf0+0));
}
