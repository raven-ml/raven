#define INFINITY (__builtin_inff())
#define NAN (__builtin_nanf(""))
extern "C" __attribute__((global)) void __attribute__((amdgpu_flat_work_group_size(1, 1))) max_reduce(float* data0_-1, float* data1_-1) {
  float buf0[1];
  *(buf0+0) = ((float)(-INFINITY));
  for (int Ridx0 = 0; Ridx0 < 64; Ridx0++) {
    float val0 = (*(data0_-1+Ridx0));
    float val1 = (*(buf0+0));
    float alu1 = ((val1<val0)?val0:val1);
    *(buf0+0) = alu1;
  }
  *(data1_-1+0) = (*(buf0+0));
}
