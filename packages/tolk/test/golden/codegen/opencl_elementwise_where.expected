__kernel void elementwise_where(__global float* data0, __global float* data1) {
  int gidx0 = get_group_id(0); /* 256 */
  float val0 = (*(data0+gidx0));
  float alu0 = ((0.0f<val0)?val0:0.0f);
  *(data1+gidx0) = alu0;
}
