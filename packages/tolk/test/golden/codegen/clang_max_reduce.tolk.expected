void max_reduce(float* restrict data0_64, float* restrict data1_1) {
  float buf0[1];
  *(buf0+0) = ((float)(-__builtin_inff()));
  for (int Ridx0 = 0; Ridx0 < 64; Ridx0++) {
    float val0 = (*(data0_64+Ridx0));
    float val1 = (*(buf0+0));
    float alu1 = ((val1!=val1)?val1:val0);
    float alu2 = ((val0<val1)?val1:alu1);
    *(buf0+0) = alu2;
  }
  *(data1_1+0) = (*(buf0+0));
}
