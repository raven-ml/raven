void reduce_rows(float* restrict data0_256, float* restrict data1_8) {
  float buf0[1];
  for (int Lidx0 = 0; Lidx0 < 8; Lidx0++) {
    *(buf0+0) = 0.0f;
    for (int Ridx1 = 0; Ridx1 < 32; Ridx1++) {
      float val0 = (*(data0_256+((Lidx0<<5)+Ridx1)));
      *(buf0+0) = ((*(buf0+0))+val0);
    }
    *(data1_8+Lidx0) = (*(buf0+0));
  }
}
