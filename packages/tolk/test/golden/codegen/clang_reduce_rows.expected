void reduce_rows(float* restrict data0_-1, float* restrict data1_-1) {
  float buf0[1];
  for (int Lidx0 = 0; Lidx0 < 8; Lidx0++) {
    *(buf0+0) = 0.0f;
    for (int Ridx1 = 0; Ridx1 < 32; Ridx1++) {
      float val0 = (*(data0_-1+((Lidx0<<5)+Ridx1)));
      *(buf0+0) = ((*(buf0+0))+val0);
    }
    *(data1_-1+Lidx0) = (*(buf0+0));
  }
}
