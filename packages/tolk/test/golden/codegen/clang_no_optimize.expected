void no_optimize(float* restrict data0, float* restrict data1, float* restrict data2, const int core_id) {
  float val0 = (*(data0+core_id));
  float val1 = (*(data1+core_id));
  *(data2+core_id) = (val0+val1);
}
