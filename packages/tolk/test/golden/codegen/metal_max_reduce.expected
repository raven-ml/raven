#include <metal_stdlib>
using namespace metal;
struct args_t { device float* data0_64; device float* data1_1; };
kernel void max_reduce(constant args_t& args [[buffer(0)]], uint3 gid [[threadgroup_position_in_grid]], uint3 lid [[thread_position_in_threadgroup]]) {
  device float* data0_64 = args.data0_64; device float* data1_1 = args.data1_1;
  float buf0[1];
  *(buf0+0) = ((float)(-INFINITY));
  for (int Ridx0 = 0; Ridx0 < 64; Ridx0++) {
    float val0 = (*(data0_64+Ridx0));
    float val1 = (*(buf0+0));
    float alu1 = ((val1!=val1)?val1:val0);
    float alu2 = ((val0<val1)?val1:alu1);
    *(buf0+0) = alu2;
  }
  *(data1_1+0) = (*(buf0+0));
}
