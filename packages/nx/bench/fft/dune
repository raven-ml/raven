(executable
 (name bench_fft)
 (modules bench_fft)
 (libraries nx thumper))

(rule
 (alias bench)
 (locks %{project_root}/bench)
 (action
  (progn
   (run %{exe:bench_fft.exe} -q)
   (diff? nx_fft.thumper nx_fft.thumper.corrected))))
