(executable
 (name bench_einsum_nx)
 (modules bench_einsum_nx)
 (libraries nx thumper))

; Not attached to runtest on purpose: run with `dune build @bench`
; (or `dune exec packages/nx/bench/einsum/bench_einsum_nx.exe -- --explore`).

(rule
 (alias bench)
 ; Serialize bench rules across the workspace: concurrent suites contend
 ; for cores and distort measurements.
 (locks %{project_root}/bench)
 (action
  (progn
   (run %{exe:bench_einsum_nx.exe} -q)
   (diff? nx_einsum.thumper nx_einsum.thumper.corrected))))
