__kernel void dot_product(__global float* data0, __global float* data1, __global float* data2) {
  float acc0[1];
  *(acc0+0) = 0.0f;
  for (int Ridx0 = 0; Ridx0 < 128; Ridx0++) {
    float val0 = (*(data0+Ridx0));
    float val1 = (*(data1+Ridx0));
    *(acc0+0) = ((*(acc0+0))+(val0*val1));
  }
  *(data2+0) = (*(acc0+0));
}
