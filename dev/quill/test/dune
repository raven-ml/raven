(cram
 (package quill)
 ; TODO: the quill plugins are not loaded when running from opam ci.
 ; This works after installing quill, though, so we disable the tests in CI.
 (enabled_if false)
 (deps %{bin:quill}))
