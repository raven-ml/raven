# Raven Website

This directory contains the static website hosted at [raven-ml.dev](https://raven-ml.dev).

- `index.html` is the landing page.
- `docs/` contains documentation.
  - `guides/` holds Markdown guides.
  - `api/` provides API reference placeholders.
