# Policy Gradient

REINFORCE policy gradient algorithm on the RandomWalk environment. Uses a small neural network (built with Kaun) as the policy, trained with the `Fehu_algorithms.Reinforce` module.
