# Basic Operations

Arithmetic operations (`add`, `sub`, `mul`, `div`), element indexing, array transformations (`transpose`, `reshape`), math functions (`square`, `sqrt`), and comparison operations on Nx arrays.
