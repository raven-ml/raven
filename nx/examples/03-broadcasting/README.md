# Broadcasting

Broadcasting rules for arrays of different shapes. Shows automatic broadcasting in arithmetic operations, explicit `broadcast_to`, and `broadcast_arrays` for aligning multiple arrays.
